def n_ty_wyraz(a_1, n, r):
    wyraz = a_1 + (n-1) * r
    return wyraz


def suma(a_1, a_n, n):
    suma = 0.5 * (a_1 + a_n) * n
    return suma


def srodkowy_wyraz(x, z):
    y = (x + z) * 0.5
    return y
